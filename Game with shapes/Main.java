#include<iostream>
using namespace std;
int main()
{
int r,l,d;
std::cin>>r;
std::cin>>l;
d=2*r;
if(d==l)
{
std::cout<<"circle can be inside a square";
}
else
{
std::cout<<"circle cannot be inside a square";
}
}
